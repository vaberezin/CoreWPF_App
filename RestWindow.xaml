<Window x:Class="CoreWpfApp.RestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoreWpfApp"
        mc:Ignorable="d"
        Title="Группа ВК" Height="450" Width="800"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="12"
        FontFamily="{materialDesign:MaterialDesignFont}" Loaded="Window_Loaded"
        
        >
    <Grid>


    <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="100 px"/>
    </Grid.RowDefinitions>

    <TextBlock x:Name = "RestTextBlock" Grid.Row = "0" VerticalAlignment = "Center" HorizontalAlignment = "Center" Margin = "50" Text = "Timer Time" FontSize ="72"/>

    <Button x:Name = "ContinueWorkButton" Grid.Row = "1" VerticalAlignment = "Center" HorizontalAlignment = "Center" Height = "80" Width="200" Click = "ContinueWorkButton_Click" Content="Продолжить работу"/>


    </Grid>
</Window>
